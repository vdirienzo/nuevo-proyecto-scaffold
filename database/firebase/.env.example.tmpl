# Firebase Configuration - {{PROJECT_NAME}}
# Author: {{AUTHOR}}
#
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env.local or .env files to version control
# 2. NEXT_PUBLIC_* variables are exposed to the browser - safe for client config
# 3. Variables WITHOUT NEXT_PUBLIC_ are server-only - use for sensitive data
# 4. Admin credentials should NEVER have NEXT_PUBLIC_ prefix

# ============================================================================
# CLIENT-SIDE FIREBASE CONFIG (Safe to expose to browser)
# ============================================================================
# Get these values from Firebase Console > Project Settings > General
# https://console.firebase.google.com/project/YOUR_PROJECT/settings/general

NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# SERVER-SIDE FIREBASE ADMIN SDK (NEVER expose to browser)
# ============================================================================
# Get these values from Firebase Console > Project Settings > Service Accounts
# https://console.firebase.google.com/project/YOUR_PROJECT/settings/serviceaccounts/adminsdk
# Click "Generate new private key" and copy values from the JSON file

FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Go to Firebase Console: https://console.firebase.google.com
#
# 3. For CLIENT variables:
#    - Navigate to Project Settings > General
#    - Scroll to "Your apps" section
#    - Copy values from the Firebase SDK snippet
#
# 4. For ADMIN variables:
#    - Navigate to Project Settings > Service Accounts
#    - Click "Generate new private key"
#    - Download the JSON file
#    - Copy project_id, client_email, and private_key to .env.local
#    - IMPORTANT: Replace \n in private_key with actual newlines or keep as \n
#
# 5. Add .env.local to .gitignore (should already be there)
#
# 6. Restart your development server:
#    npm run dev
#
# ============================================================================
# SECURITY CHECKLIST
# ============================================================================
# [ ] .env.local is in .gitignore
# [ ] No NEXT_PUBLIC_ prefix on admin credentials
# [ ] Private key is properly escaped
# [ ] Service account has minimal required permissions
# [ ] Firebase Security Rules are configured for production
# [ ] Authentication is enabled in Firebase Console
# [ ] Firestore indexes are created for your queries
