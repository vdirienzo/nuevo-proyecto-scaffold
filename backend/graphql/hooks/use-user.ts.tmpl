/**
 * use-user.ts - User GraphQL Hooks
 *
 * Author: {{AUTHOR}}
 * Project: {{PROJECT_NAME}}
 *
 * Generated by GraphQL Code Generator
 * This is an example of the generated hooks
 */

import { useQuery, useMutation } from '@apollo/client';
import { gql } from '@apollo/client';

// Example query hook (will be generated by codegen)
const ME_QUERY = gql`
  query Me {
    me {
      id
      email
      name
      createdAt
      updatedAt
    }
  }
`;

const GET_USER_QUERY = gql`
  query GetUser($id: ID!) {
    user(id: $id) {
      id
      email
      name
      createdAt
      updatedAt
    }
  }
`;

const LIST_USERS_QUERY = gql`
  query ListUsers($skip: Int, $take: Int) {
    users(skip: $skip, take: $take) {
      id
      email
      name
      createdAt
      updatedAt
    }
  }
`;

/**
 * Hook to get current user
 */
export function useMe() {
  return useQuery(ME_QUERY);
}

/**
 * Hook to get user by ID
 */
export function useUser(id: string) {
  return useQuery(GET_USER_QUERY, {
    variables: { id },
    skip: !id,
  });
}

/**
 * Hook to list users
 */
export function useUsers(skip = 0, take = 10) {
  return useQuery(LIST_USERS_QUERY, {
    variables: { skip, take },
  });
}
