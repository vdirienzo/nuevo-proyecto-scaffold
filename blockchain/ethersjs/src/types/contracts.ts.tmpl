/**
 * @fileoverview Contract-specific types
 * @author {{AUTHOR}}
 *
 * Note: These types will be auto-generated by TypeChain from contract ABIs.
 * This file provides manual type definitions as a fallback.
 */

/**
 * Token contract types
 */
export namespace TokenContract {
  export interface TokenInfo {
    name: string;
    symbol: string;
    decimals: number;
    totalSupply: bigint;
    maxSupply: bigint;
  }

  export interface Transfer {
    from: string;
    to: string;
    value: bigint;
  }

  export interface Approval {
    owner: string;
    spender: string;
    value: bigint;
  }
}

/**
 * NFT contract types
 */
export namespace NFTContract {
  export interface NFTInfo {
    name: string;
    symbol: string;
    maxSupply: bigint;
    mintPrice: bigint;
  }

  export interface Transfer {
    from: string;
    to: string;
    tokenId: bigint;
  }

  export interface Approval {
    owner: string;
    approved: string;
    tokenId: bigint;
  }

  export interface ApprovalForAll {
    owner: string;
    operator: string;
    approved: boolean;
  }
}

/**
 * Staking contract types
 */
export namespace StakingContract {
  export interface StakingConfig {
    stakingToken: string;
    rewardToken: string;
    rewardRate: bigint;
    minStakeAmount: bigint;
    lockPeriod: bigint;
  }

  export interface UserStake {
    amount: bigint;
    rewardPerTokenPaid: bigint;
    rewards: bigint;
    stakedAt: bigint;
  }

  export interface Staked {
    user: string;
    amount: bigint;
  }

  export interface Withdrawn {
    user: string;
    amount: bigint;
  }

  export interface RewardPaid {
    user: string;
    reward: bigint;
  }
}

/**
 * Governance contract types
 */
export namespace GovernanceContract {
  export interface Proposal {
    id: bigint;
    proposer: string;
    targets: string[];
    values: bigint[];
    signatures: string[];
    calldatas: string[];
    startBlock: bigint;
    endBlock: bigint;
    description: string;
  }

  export interface ProposalVotes {
    againstVotes: bigint;
    forVotes: bigint;
    abstainVotes: bigint;
  }

  export interface ProposalCreated {
    proposalId: bigint;
    proposer: string;
    targets: string[];
    values: bigint[];
    signatures: string[];
    calldatas: string[];
    voteStart: bigint;
    voteEnd: bigint;
    description: string;
  }

  export interface VoteCast {
    voter: string;
    proposalId: bigint;
    support: number;
    weight: bigint;
    reason: string;
  }
}

/**
 * Multicall types
 */
export namespace Multicall {
  export interface Call {
    target: string;
    callData: string;
  }

  export interface Call3 {
    target: string;
    allowFailure: boolean;
    callData: string;
  }

  export interface Result {
    success: boolean;
    returnData: string;
  }
}
