/**
 * App Constants
 *
 * Author: {{AUTHOR}}
 */

import {API_URL as ENV_API_URL, API_TIMEOUT as ENV_API_TIMEOUT} from '@env';

// API Configuration
export const API_URL = ENV_API_URL || 'http://localhost:8000/api';
export const API_TIMEOUT = parseInt(ENV_API_TIMEOUT, 10) || 30000;

// App Configuration
export const APP_NAME = '{{PROJECT_NAME}}';
export const APP_VERSION = '1.0.0';

// Validation
export const VALIDATION = {
  EMAIL_REGEX: /\S+@\S+\.\S+/,
  PASSWORD_MIN_LENGTH: 6,
  NAME_MIN_LENGTH: 2,
};

// Storage Keys
export const STORAGE_KEYS = {
  TOKEN: '@auth_token',
  USER: '@user_data',
  THEME: '@theme_preference',
  LANGUAGE: '@language',
} as const;

// Date Formats
export const DATE_FORMATS = {
  DISPLAY: 'MMM DD, YYYY',
  FULL: 'MMMM DD, YYYY HH:mm',
  TIME: 'HH:mm',
};

// Pagination
export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 20,
  MAX_PAGE_SIZE: 100,
};
