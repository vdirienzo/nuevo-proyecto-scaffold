/**
 * constants.ts - Shared constants for design systems
 *
 * Autor: {{AUTHOR}}
 * Project: {{PROJECT_NAME}}
 */

import type { DesignSystemName, DesignTokens } from './types';

/**
 * Available design systems
 */
export const DESIGN_SYSTEMS: readonly DesignSystemName[] = [
  'glassmorphism',
  'neumorphism',
  'neo-brutalism',
  'claymorphism',
  'geist',
] as const;

/**
 * Design system display names
 */
export const DESIGN_SYSTEM_NAMES: Record<DesignSystemName, string> = {
  glassmorphism: 'Glassmorphism',
  neumorphism: 'Neumorphism',
  'neo-brutalism': 'Neo-Brutalism',
  claymorphism: 'Claymorphism',
  geist: 'Geist',
};

/**
 * Design system descriptions
 */
export const DESIGN_SYSTEM_DESCRIPTIONS: Record<DesignSystemName, string> = {
  glassmorphism: 'Frosted glass effect with blur and transparency',
  neumorphism: 'Soft shadows and subtle depth for modern UIs',
  'neo-brutalism': 'Bold, sharp, and high-contrast with thick borders',
  claymorphism: 'Soft, rounded, and playful like modeling clay',
  geist: 'Minimal, precise, and elegant Swiss design',
};

/**
 * Default design tokens (base values)
 */
export const DEFAULT_TOKENS: Partial<DesignTokens> = {
  spacing: {
    0: '0',
    1: '0.25rem',
    2: '0.5rem',
    3: '0.75rem',
    4: '1rem',
    5: '1.25rem',
    6: '1.5rem',
    8: '2rem',
    10: '2.5rem',
    12: '3rem',
    16: '4rem',
    20: '5rem',
    24: '6rem',
    32: '8rem',
    40: '10rem',
    48: '12rem',
    56: '14rem',
    64: '16rem',
  },
  breakpoints: {
    mobile: '640px',
    tablet: '768px',
    desktop: '1024px',
    wide: '1280px',
  },
  zIndex: {
    base: 0,
    dropdown: 100,
    sticky: 200,
    fixed: 300,
    modalBackdrop: 400,
    modal: 500,
    popover: 600,
    tooltip: 700,
  },
  animation: {
    duration: {
      instant: '100ms',
      fast: '200ms',
      normal: '300ms',
      slow: '500ms',
      slower: '800ms',
    },
    easing: {
      linear: 'linear',
      easeIn: 'cubic-bezier(0.4, 0, 1, 1)',
      easeOut: 'cubic-bezier(0, 0, 0.2, 1)',
      easeInOut: 'cubic-bezier(0.4, 0, 0.2, 1)',
      spring: 'cubic-bezier(0.68, -0.55, 0.265, 1.55)',
    },
  },
};

/**
 * Tailwind breakpoints (for responsive utilities)
 */
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
} as const;

/**
 * Common font families
 */
export const FONT_FAMILIES = {
  sans: 'ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"',
  serif: 'ui-serif, Georgia, Cambria, "Times New Roman", Times, serif',
  mono: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace',
  geist: '"Geist Sans", ui-sans-serif, system-ui, sans-serif',
  inter: '"Inter", ui-sans-serif, system-ui, sans-serif',
} as const;

/**
 * Component size mappings
 */
export const COMPONENT_SIZES = {
  xs: 'xs',
  sm: 'sm',
  md: 'md',
  lg: 'lg',
  xl: 'xl',
} as const;

/**
 * Component variants
 */
export const COMPONENT_VARIANTS = {
  primary: 'primary',
  secondary: 'secondary',
  outline: 'outline',
  ghost: 'ghost',
  danger: 'danger',
  success: 'success',
  warning: 'warning',
  info: 'info',
} as const;

/**
 * Animation durations in milliseconds
 */
export const ANIMATION_DURATIONS = {
  instant: 100,
  fast: 200,
  normal: 300,
  slow: 500,
  slower: 800,
} as const;

/**
 * Common border radius values
 */
export const BORDER_RADIUS = {
  none: '0',
  sm: '0.125rem',
  base: '0.25rem',
  md: '0.375rem',
  lg: '0.5rem',
  xl: '0.75rem',
  '2xl': '1rem',
  '3xl': '1.5rem',
  full: '9999px',
} as const;

/**
 * Common shadow values
 */
export const SHADOWS = {
  none: 'none',
  sm: '0 1px 2px 0 rgb(0 0 0 / 0.05)',
  base: '0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)',
  md: '0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)',
  lg: '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)',
  xl: '0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)',
  '2xl': '0 25px 50px -12px rgb(0 0 0 / 0.25)',
  inner: 'inset 0 2px 4px 0 rgb(0 0 0 / 0.05)',
} as const;

/**
 * Common transition properties
 */
export const TRANSITIONS = {
  all: 'all',
  colors: 'background-color, border-color, color, fill, stroke',
  opacity: 'opacity',
  shadow: 'box-shadow',
  transform: 'transform',
} as const;

/**
 * LocalStorage keys
 */
export const STORAGE_KEYS = {
  theme: '{{PROJECT_NAME}}-theme',
  designSystem: '{{PROJECT_NAME}}-design-system',
  preferences: '{{PROJECT_NAME}}-preferences',
} as const;

/**
 * CSS custom property prefixes
 */
export const CSS_VAR_PREFIX = {
  color: '--color',
  spacing: '--spacing',
  font: '--font',
  radius: '--radius',
  shadow: '--shadow',
  duration: '--duration',
  easing: '--easing',
} as const;

/**
 * Accessible color contrast ratios (WCAG)
 */
export const CONTRAST_RATIOS = {
  AA_NORMAL: 4.5, // WCAG AA for normal text
  AA_LARGE: 3, // WCAG AA for large text (18pt+)
  AAA_NORMAL: 7, // WCAG AAA for normal text
  AAA_LARGE: 4.5, // WCAG AAA for large text
} as const;

/**
 * Common aspect ratios
 */
export const ASPECT_RATIOS = {
  square: '1 / 1',
  video: '16 / 9',
  portrait: '3 / 4',
  landscape: '4 / 3',
  ultrawide: '21 / 9',
} as const;

/**
 * Focus ring styles
 */
export const FOCUS_RING = {
  default: 'focus:outline-none focus:ring-2 focus:ring-offset-2',
  primary: 'focus:ring-primary-500',
  error: 'focus:ring-error-500',
  none: 'focus:outline-none',
} as const;

/**
 * Screen reader only utility class
 */
export const SR_ONLY = 'sr-only';

/**
 * Common icon sizes
 */
export const ICON_SIZES = {
  xs: '0.75rem',
  sm: '1rem',
  md: '1.25rem',
  lg: '1.5rem',
  xl: '2rem',
  '2xl': '2.5rem',
} as const;

/**
 * Loading spinner sizes
 */
export const SPINNER_SIZES = {
  sm: '1rem',
  md: '1.5rem',
  lg: '2rem',
  xl: '3rem',
} as const;

/**
 * Default animation config
 */
export const DEFAULT_ANIMATION_CONFIG = {
  duration: 300,
  easing: 'cubic-bezier(0.4, 0, 0.2, 1)',
  delay: 0,
} as const;
