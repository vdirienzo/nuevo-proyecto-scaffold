# Environment Variables para {{PROJECT_NAME}}
# Autor: {{AUTHOR}}
#
# Copiar este archivo a .env y ajustar los valores según tu entorno
# IMPORTANTE: NO commitear .env con valores sensibles

# =============================================================================
# Application Settings
# =============================================================================

# Modo de debug (true para desarrollo, false para producción)
APP_DEBUG=true

# Nivel de logging de Rust
# Opciones: error, warn, info, debug, trace
RUST_LOG=debug

# Backtrace para debugging de Rust (1 = enabled, 0 = disabled)
RUST_BACKTRACE=1

# Puerto de la aplicación (si usa servidor interno)
APP_PORT=3000

# Nombre de la aplicación (usado en logs y títulos)
APP_NAME={{PROJECT_NAME}}

# Versión de la aplicación
APP_VERSION=1.0.0

# =============================================================================
# API Configuration (Opcional)
# =============================================================================

# Base URL para API remota (si la app se conecta a un backend)
# Dejar vacío si la app es standalone
API_BASE_URL=http://localhost:8080

# API Key para autenticación (si es necesario)
# API_KEY=your_api_key_here

# Timeout de requests HTTP (en segundos)
API_TIMEOUT=30

# =============================================================================
# Database Configuration (Opcional)
# =============================================================================

# URL de conexión a PostgreSQL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/{{PROJECT_NAME}}

# Database connection pool settings
DB_POOL_SIZE=10
DB_POOL_TIMEOUT=5

# Credenciales individuales (alternativa a DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME={{PROJECT_NAME}}
DB_USER=postgres
DB_PASSWORD=password

# =============================================================================
# Redis Configuration (Opcional)
# =============================================================================

# URL de conexión a Redis
# REDIS_URL=redis://localhost:6379

# Redis password
# REDIS_PASSWORD=redispass

# Redis database number (0-15)
# REDIS_DB=0

# =============================================================================
# Security Settings
# =============================================================================

# Secret key para JWT, encryption, etc.
# GENERAR EN PRODUCCIÓN: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_change_in_production

# JWT expiration time (en segundos)
# 3600 = 1 hora, 86400 = 1 día
JWT_EXPIRATION=86400

# Allowed origins para CORS (si la app expone API)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# =============================================================================
# External Services (Opcional)
# =============================================================================

# AWS Configuration
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Sentry for error tracking
# SENTRY_DSN=https://your_sentry_dsn_here

# Analytics
# ANALYTICS_ID=your_analytics_id

# =============================================================================
# Feature Flags
# =============================================================================

# Habilitar features opcionales
FEATURE_TELEMETRY=false
FEATURE_AUTO_UPDATE=true
FEATURE_NOTIFICATIONS=true

# =============================================================================
# Development Tools
# =============================================================================

# Habilitar DevTools en la aplicación
DEVTOOLS_ENABLED=true

# Hot reload (si está soportado)
HOT_RELOAD=true

# =============================================================================
# Logging and Monitoring
# =============================================================================

# Archivo de log (path relativo o absoluto)
LOG_FILE=./logs/app.log

# Nivel de log mínimo para archivo
LOG_FILE_LEVEL=info

# Rotación de logs (en MB)
LOG_MAX_SIZE=10

# Logs a conservar
LOG_MAX_BACKUPS=5

# =============================================================================
# Docker Specific (usado por docker-compose.yml)
# =============================================================================

# Display para X11 (Linux GUI)
DISPLAY=:0

# User ID y Group ID (para permisos de volúmenes)
UID=1000
GID=1000

# =============================================================================
# Testing
# =============================================================================

# URL de test database
TEST_DATABASE_URL=postgresql://postgres:password@localhost:5432/{{PROJECT_NAME}}_test

# Modo de test
TEST_MODE=false

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
#
# 1. NO commitear este archivo con valores reales a git
# 2. Agregar .env a .gitignore
# 3. Usar secrets manager en producción (AWS Secrets Manager, Vault, etc.)
# 4. Rotar SECRET_KEY y API keys regularmente
# 5. Usar variables de entorno del sistema en CI/CD
# 6. Nunca loggear valores sensibles
# 7. Validar y sanitizar todas las variables antes de usar
#
# =============================================================================
