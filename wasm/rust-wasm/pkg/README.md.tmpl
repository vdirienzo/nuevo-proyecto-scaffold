# {{PROJECT_NAME}} WASM Package

This directory contains the compiled WebAssembly module generated by `wasm-pack`.

## Build

To generate the WASM package:

```bash
wasm-pack build --target web
```

## Contents

After building, this directory will contain:

- `{{PROJECT_NAME}}_wasm.js` - JavaScript bindings
- `{{PROJECT_NAME}}_wasm_bg.wasm` - Compiled WebAssembly binary
- `{{PROJECT_NAME}}_wasm.d.ts` - TypeScript definitions
- `package.json` - NPM package metadata

## Usage

The package can be imported in JavaScript/TypeScript:

```javascript
import * as wasm from './pkg/{{PROJECT_NAME}}_wasm.js';

wasm.init();
const hash = wasm.sha256_hex(data);
```

## Distribution

Publish to NPM:

```bash
cd pkg
npm publish
```

---

Author: {{AUTHOR}}
